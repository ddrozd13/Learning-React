import { StyleSheet} from 'react-native';

const gStyle = StyleSheet.create({
  main: {
    flex: 1,
    padding: 20,
    paddingTop: 20,
  },
  title: {
    fontSize: 20,
    color: '#333',
    fontFamily: 'mt-bold',
    textAlign: 'center',
  }
})

export default gStyle;
